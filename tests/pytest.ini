[pytest]
addopts = -ra --strict-markers
testpaths = tests
markers =
    slow: slow/compute-heavy tests
    gpu: tests requiring CUDA
    net: tests requiring network access
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
    
